VITE_API_BASE_URL=https://buymore-api-production.up.railway.app

# InTouch SDK (client-side) — mettre le vrai Secure Code InTouch
# InTouch : config chargée depuis /api/payments/intouch/sdk-config — pas de vars ici
